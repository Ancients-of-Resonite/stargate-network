import { Header } from "@/components/header";

export default async function Home() {
  // const data = await fetch("/api/stargates");
  // const stargates = data.json();

  return (
    <div className="bg-zinc-50 font-sans dark:bg-black">
      <Header />
      <main className="">
      </main>
    </div>
  );
}
